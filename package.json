{
  "name": "temp-mail-browser",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny SMTP temp mail receiver with SQLite storage and browser UI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0",
    "mailparser": "^3.7.2",
    "smtp-server": "^3.15.0"
  }
}
